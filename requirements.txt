# ITSthe1.ID API - runtime dependencies
# Install with:  pip install -r requirements.txt

Flask==3.1.1
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.44
waitress==3.0.2
pyodbc==5.3.0
pillow==12.0.0
googletrans==4.0.2
tzlocal==5.3.1
requests==2.32.5
# System tray icon for the windowless build (app/tray.py). Without it the API
# still runs, just with no tray icon and no way to quit but Task Manager.
pystray==0.19.5

# Build-time only (creating dist/run.exe). Not needed on a server that just
# runs the pre-built executable.
pyinstaller==6.14.2
